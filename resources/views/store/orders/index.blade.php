@extends('layouts.store')
@section('title','Pesanan Saya')
@section('content')<section class="page-shell min-h-[60vh] py-12"><p class="eyebrow">Akun saya</p><h1 class="section-title mt-3">Pesanan.</h1><div class="mt-9 space-y-3">@forelse($orders as $order)<a href="{{ route('orders.show',$order) }}" class="card grid gap-3 p-5 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><strong>{{ $order->order_number }}</strong><p class="text-sm text-ink/55">{{ $order->created_at->translatedFormat('d F Y') }} · {{ $order->items_count }} produk</p></div><span class="badge">{{ $order->status }}</span><strong>Rp{{ number_format($order->total,0,',','.') }}</strong></a>@empty<div class="py-24 text-center"><p class="font-display text-3xl">Belum ada pesanan.</p><a class="btn mt-6" href="{{ route('products.index') }}">Mulai belanja</a></div>@endforelse</div>{{ $orders->links() }}</section>@endsection
